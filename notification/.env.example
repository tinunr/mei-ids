# SMTP configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
# Use 'tls' for STARTTLS (port 587), 'ssl' for SMTPS (port 465), or 'none'
SMTP_ENCRYPTION=tls
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
# Optional custom From header
SMTP_FROM=Notifications <no-reply@example.com>
